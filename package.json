{
  "devDependencies": {
    "sass": "^1.54.8",
    "vite": "^3.0.9"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.0"
  }
}
